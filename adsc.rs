use std::fs;
use std::io::Write;

extern crate rand;

//use rand::Rng;

fn main() {

    let N = 50;
    let orderList: [T;N];

    for x in 0..N{
        orderList[x] = Vec::new();
        /*
        while(){

        }
        */
    }
}
